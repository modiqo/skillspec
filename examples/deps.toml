# Example dependency manifest placeholder for examples/durable-executor/skill.spec.yml.
# Real released rote flows should replace this with the manifest format expected
# by their target rote runtime.
