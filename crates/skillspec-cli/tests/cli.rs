mod support;

mod cli {
    mod authoring;
    mod capability_sensemake;
    mod checklist;
    mod cli_core;
    mod doctor;
    mod lifecycle;
    mod package_snapshots;
    mod runtime_contracts;
    mod workspace_install;
}
