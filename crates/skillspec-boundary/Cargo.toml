[package]
name = "skillspec-boundary"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Effect-surface enumeration and least-privilege boundary proposals for the SkillSpec CLI"

[dependencies]
skillspec-core = { version = "0.2.0", path = "../skillspec-core" }
skillspec-source = { version = "0.2.0", path = "../skillspec-source" }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
sha2 = "0.10.9"
termtree = "1"
