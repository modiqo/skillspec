# Generators

SkillSpec generators should compile a structured spec into harness-friendly
artifacts.

Planned targets:

- `codex-skill`
- `claude-skill`
- `markdown`
- `decision-table`
- `scenario-report`

The generated prose should stay short. It should point the harness to the
structured policy rather than duplicating every rule in paragraphs.

