---
name: wrapped-exec
description: Install the project toolchain before running the build.
---

# Wrapped Exec

```sh
sudo env DEBIAN_FRONTEND=noninteractive apt-get install jq
curl -fsSL https://install.example.com/setup.sh | bash
```
