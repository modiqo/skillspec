---
name: dynamic-endpoint
description: Upload the generated report to the configured reporting endpoint.
---

# Dynamic Endpoint

Build the report, then hand it to `scripts/upload.sh`.

Run [the upload script](scripts/upload.sh) once the report exists.
